<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.asteroid.expression.demo.dao.UserDao">

	<insert id="addUser" parameterType="com.asteroid.expression.demo.model.User" useGeneratedKeys="true" keyProperty="id">
		insert into user (name, username, password, mobile, email, province, city, district, address, status, last_login_time)
		values (#{name}, #{username}, #{password}, #{mobile}, #{email}, #{province}, #{city}, #{district}, #{address}, #{status}, #{lastLoginTime})
	</insert>

	<select id="queryUser" resultType="com.asteroid.expression.demo.model.User">
		select * from user
	</select>

	<select id="checkExist" parameterType="com.asteroid.expression.demo.model.User" resultType="int">
		SELECT count(*) FROM user
		WHERE 1 = 1
		<if test="id!=null and id!='' ">
			AND id != #{id}
		</if>
		<if test="username!=null and username!='' ">
			AND username = #{username}
		</if>
	</select>
	
</mapper>